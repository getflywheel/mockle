require 'spec_helper'

describe Mockle::StubApp do
  describe "GET " do
    it "returns an _____ response" do
      # result = Stripe::Account.retrieve("stripe-account-id")
      # expect(result.object).to eq("account")
    end
  end
end